version: '3'

services:
    frontend:
        build: 
            args: 
                APP_ENV: prod  # dev or prod
    backend:
        build:
            args:
                APP_ENV: prod  # dev or prod
                BUILD_PLATFORM: base-arm7  # base or base-arm7
